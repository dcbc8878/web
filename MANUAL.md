# คู่มือใช้งานระบบหลังบ้าน — เด็กชายบัญชี

เว็บไซต์: **www.dcbc.co.th**
หน้าหลังบ้าน: **www.dcbc.co.th/adminupload**

---

## สารบัญ

1. [เข้าสู่ระบบ](#1-เข้าสู่ระบบ)
2. [แท็บเอกสาร](#2-แท็บเอกสาร)
3. [แท็บรีวิว](#3-แท็บรีวิว)
4. [แท็บรหัสลูกค้า](#4-แท็บรหัสลูกค้า)
5. [แท็บโปรแกรม](#5-แท็บโปรแกรม)
6. [แท็บบทความ](#6-แท็บบทความ)
7. [แท็บผู้ใช้](#7-แท็บผู้ใช้)
8. [ปัญหาที่พบบ่อย](#8-ปัญหาที่พบบ่อย)

---

## 1. เข้าสู่ระบบ

เปิด **www.dcbc.co.th/adminupload** แล้วกรอกอีเมลและรหัสผ่าน

- ระบบจะจำการเข้าสู่ระบบไว้ ไม่ต้องล็อกอินใหม่ทุกครั้ง
- มุมขวาบนแสดงชื่อและระดับสิทธิ์ของคุณ
- **แท็บที่เห็นขึ้นอยู่กับสิทธิ์ที่ได้รับ** ถ้าเห็นไม่ครบทุกแท็บถือว่าปกติ

> **หน้านี้ไม่มีลิงก์จากหน้าเว็บใดๆ** และตั้งค่าไม่ให้ Google เก็บข้อมูล ต้องพิมพ์ URL เองเท่านั้น

---

## 2. แท็บเอกสาร

จัดการแบบฟอร์มที่ลูกค้าดาวน์โหลดได้จากหน้า **/portal**

### เพิ่มเอกสารทีละไฟล์
กรอกชื่องาน → คำอธิบาย → เลือกไฟล์ → กด **เพิ่มเอกสาร**

### เพิ่มหลายไฟล์พร้อมกัน
ใช้กล่อง **"เพิ่มเอกสารหลายไฟล์พร้อมกัน"** เลือกได้ทีละหลายไฟล์
ระบบเติมชื่อและคำอธิบายให้อัตโนมัติสำหรับไฟล์ที่รู้จัก แก้ได้ก่อนกดอัปโหลด

### จัดลำดับ
ใช้ปุ่มลูกศร **▲ ▼** ข้างเอกสารแต่ละรายการ ลำดับที่จัดจะตรงกับที่ลูกค้าเห็นในหน้า /portal

### แก้ไข / ลบ
- **แก้ไข** — ข้อมูลเดิมจะเด้งขึ้นฟอร์มด้านบน ถ้าไม่ต้องการเปลี่ยนไฟล์ก็เว้นช่องไฟล์ว่างไว้
- **ลบ** — ลบทั้งข้อมูลและไฟล์ออกจากระบบถาวร

> **ชื่อไฟล์ภาษาไทยใช้ได้** ระบบแปลงชื่อสำหรับจัดเก็บให้เอง แต่ตอนลูกค้าโหลดจะได้ชื่อไทยเหมือนเดิม

---

## 3. แท็บรีวิว

### ลิงก์รับรีวิว
ลิงก์ **www.dcbc.co.th/review** สำหรับส่งให้ลูกค้าเขียนรีวิว

- **คัดลอกลิงก์** — คัดลอกไปวางในแชท
- **ดาวน์โหลด QR Code** — ได้ไฟล์ภาพ เอาไปพิมพ์ติดหน้าร้านหรือใส่ในเอกสารได้

### อนุมัติรีวิว
รีวิวที่ลูกค้าส่งเข้ามาจะเป็น **"รอตรวจสอบ"** ก่อนเสมอ ยังไม่ขึ้นเว็บ

- **อนุมัติ** — ขึ้นแสดงบนหน้าแรก
- **ปฏิเสธ** — ไม่แสดง แต่ยังเก็บไว้ดูได้
- **ลบ** — ลบถาวร

### ⚠️ กฎสำคัญ: ต้องมีอย่างน้อย 5 รีวิว

**ถ้ารีวิวที่อนุมัติแล้วมีน้อยกว่า 5 รายการ ส่วนรีวิวจะไม่แสดงบนหน้าเว็บเลย**
(รวมถึงเมนู "รีวิวจากลูกค้า" ก็จะหายไปด้วย)

ทำแบบนี้เพื่อไม่ให้เว็บดูเหมือนมีลูกค้าน้อย ตัวเลขด้านบนบอกว่าตอนนี้มีกี่รายการแล้ว

---

## 4. แท็บรหัสลูกค้า

รหัส 4 หลักสำหรับให้ลูกค้าเข้าหน้า **/portal**

### เพิ่มรหัส
กรอกรหัส 4 ตัว (ตัวอักษรหรือตัวเลข) + ชื่อลูกค้าสำหรับจดจำ → กด **เพิ่มรหัส**

### จัดการ
- **แก้ไข** — เปลี่ยนรหัสหรือชื่อลูกค้า
- **ปิดใช้งาน** — รหัสใช้ไม่ได้ชั่วคราว แต่ยังเก็บไว้ (เหมาะกับลูกค้าที่หยุดใช้บริการชั่วคราว)
- **ลบ** — ลบถาวร

> **แนะนำ:** ออกรหัสแยกต่อลูกค้าแต่ละราย จะได้ปิดเฉพาะรายที่เลิกใช้บริการ โดยไม่กระทบลูกค้ารายอื่น

---

## 5. แท็บโปรแกรม

จัดการเวอร์ชันของโปรแกรมช่วยดาวน์โหลด

หน้าดาวน์โหลด: **www.dcbc.co.th/program/downloader** (ไม่มีลิงก์จากหน้าไหน ต้องส่งให้ลูกค้าเอง)

### อัปโหลดเวอร์ชันใหม่
กรอกเลขเวอร์ชัน (เช่น `1.0.3`) → รายละเอียดที่แก้ → เลือกไฟล์ → กด **อัปโหลดเวอร์ชัน**

**ติ๊ก "เผยแพร่ทันที"** = ลูกค้าเห็นเลย
**ไม่ติ๊ก** = เก็บไว้ก่อน ยังไม่มีใครเห็น (เหมาะกับตอนเตรียมไฟล์ไว้ล่วงหน้า)

### ⚠️ ข้อควรระวังเรื่องลำดับเวอร์ชัน

ระบบยึด **เวลาที่อัปโหลด** ไม่ได้เทียบตัวเลขเวอร์ชัน

ถ้าอัปโหลด 1.0.5 แล้วมาอัปโหลด 1.0.4 ทีหลัง → **ระบบจะถือว่า 1.0.4 คือตัวล่าสุด**

ป้าย **"ใช้งานอยู่"** บอกว่าเวอร์ชันไหนที่ลูกค้าเห็นจริง ถ้าผิดให้กด "หยุดเผยแพร่" ตัวที่ไม่ต้องการ

---

## 6. แท็บบทความ

เขียนบทความลงเว็บ ช่วยให้ถูกค้นเจอใน Google และถูก AI อ้างอิง

### ทำไมต้องลงเว็บ ไม่ลง Facebook อย่างเดียว

Facebook บล็อกไม่ให้ Google และ AI เก็บข้อมูล → คอนเทนต์ที่ลงเฉพาะ Facebook แทบไม่ช่วยให้คนใหม่ค้นเจอเรา
เอาโพสต์เดิมมาลงเว็บด้วย ใช้เวลาเพิ่มไม่กี่นาที แต่ทำงานให้ตลอด 24 ชม.

### วิธีเขียน

| ช่อง | คำอธิบาย |
|---|---|
| **หัวข้อ** | ตั้งให้ตรงกับที่คนพิมพ์ค้นหาจริง |
| **ลิงก์หน้า (slug)** | ภาษาอังกฤษ ตัวเล็ก ใช้ขีดกลาง เช่น `vat-threshold-2569` |
| **คำโปรย** | สรุป 1-2 บรรทัด แสดงในหน้ารวมและผลค้นหา Google |
| **เนื้อหา** | วางข้อความจากโพสต์ Facebook แล้วเกลา |
| **ลิงก์ Facebook** | ใส่ไว้อ้างอิงได้ (ไม่บังคับ) |
| **รูปหน้าปก** | ช่วยให้น่าคลิกมากขึ้น |

### 💡 หัวข้อสำคัญที่สุด

โพสต์ Facebook กับบทความเว็บตั้งหัวข้อคนละแบบ

- ❌ แบบ Facebook: *"รู้ยังว่าจด VAT ตอนไหน 👀"*
- ✅ แบบเว็บ: *"รายได้เท่าไหร่ต้องจดทะเบียน VAT? สรุปเกณฑ์ปี 2569"*

แบบหลังคือสิ่งที่คนพิมพ์ค้นหาจริง

### จัดรูปแบบเนื้อหา

```
## หัวข้อใหญ่
### หัวข้อย่อย

- รายการที่ 1
- รายการที่ 2

**ตัวหนา**
```

วาง URL ลงไปเฉยๆ ระบบทำให้กดได้เอง

### แทรกรูปในเนื้อหา

1. **คลิกในช่องเนื้อหาตรงตำแหน่งที่อยากให้รูปอยู่**
2. กด **"เลือกรูปเพื่อแทรก"** (เลือกทีละหลายรูปได้)
3. ระบบแทรกให้ตรงตำแหน่งนั้นเลย

ใส่คำบรรยายใต้รูปได้โดยพิมพ์ในวงเล็บเหลี่ยม:
```
![ตัวอย่างแบบฟอร์ม ภ.พ.30](https://...)
```

### ⚠️ บทความไม่ขึ้นทันที

ระบบสร้างหน้าเว็บใหม่ **ทุก 1 ชั่วโมง**

ถ้าไม่อยากรอ สั่งให้สร้างเดี๋ยวนี้ได้:
GitHub → repo `dcbc8878/web` → แท็บ **Actions** → **Deploy to GitHub Pages** → **Run workflow**

รอ 1-2 นาที แล้วเปิด **www.dcbc.co.th/articles**

> ที่ทำแบบนี้เพราะ AI ส่วนใหญ่อ่านเฉพาะ HTML ธรรมดา ไม่รันโปรแกรมในหน้าเว็บ
> ระบบจึงต้องสร้างหน้าจริงไว้ล่วงหน้า ถ้าทำแบบโหลดสดตอนเปิดหน้า AI จะอ่านไม่เห็นเลย

---

## 7. แท็บผู้ใช้

**เฉพาะเจ้าของระบบเท่านั้นที่เห็นแท็บนี้**

### สิทธิ์ที่กำหนดได้

| สิทธิ์ | เข้าถึงอะไร |
|---|---|
| เอกสาร | เพิ่ม/แก้/ลบ/จัดลำดับเอกสาร |
| รีวิว | อนุมัติ ปฏิเสธ ลบรีวิว |
| รหัสลูกค้า | ดูและจัดการรหัสเข้าหน้า /portal |
| โปรแกรม | อัปโหลดและจัดการเวอร์ชันโปรแกรม |
| บทความ | เขียนและจัดการบทความ |

### เชิญผู้ใช้ใหม่ (2 ขั้นตอน)

**ขั้นที่ 1 — กำหนดสิทธิ์ล่วงหน้า (ในหน้าแอดมิน)**

กรอกอีเมล → ชื่อที่แสดง → ติ๊กสิทธิ์ที่จะให้ → กด **บันทึกคำเชิญ**

**ขั้นที่ 2 — ส่งอีเมลคำเชิญ (ใน Supabase)**

1. Supabase Dashboard → **Authentication** → **Users**
2. กด **Invite user** → ใส่อีเมลเดียวกับขั้นที่ 1 → ส่ง
3. ผู้ถูกเชิญได้อีเมล กดลิงก์แล้ว **ตั้งรหัสผ่านของตัวเอง**
4. เมื่อตั้งเสร็จ บัญชีขึ้นในรายชื่อพร้อมสิทธิ์ที่กำหนดไว้ทันที

> **ข้อดีของวิธีนี้:** ไม่ต้องมีรหัสผ่านกลางที่หลายคนรู้ และไม่ต้องส่งรหัสผ่านทางแชท
> แต่ละคนตั้งรหัสของตัวเอง ถ้าใครลาออกก็ระงับเฉพาะบัญชีนั้นได้

> **ทำไมส่งอีเมลจากหน้าเว็บไม่ได้:** การสร้างผู้ใช้และส่งคำเชิญต้องใช้กุญแจระดับผู้ดูแลระบบ
> ถ้าฝังกุญแจนั้นไว้ในหน้าเว็บ ใครก็ตามที่เปิดดูโค้ดหน้าเว็บจะยึดระบบทั้งหมดได้

### ⚠️ ถ้าอีเมลคำเชิญไม่ถูกส่ง

Supabase มีโควตาส่งอีเมลฟรีจำกัดมาก (ประมาณ 2-4 ฉบับต่อชั่วโมง) และบางครั้งเข้าถังขยะ

- ลองดูใน **Junk / Spam** ก่อน
- ถ้าจะใช้งานจริงจัง ควรตั้งค่า **Custom SMTP** ใน Supabase → Project Settings → Authentication → SMTP Settings
- ทางลัดกรณีเร่งด่วน: สร้างบัญชีตรงๆ ด้วย **Add user** แล้วตั้งรหัสชั่วคราว จากนั้นให้เจ้าตัวเปลี่ยนรหัสเองทันทีที่เข้าใช้ครั้งแรก

### จัดการผู้ใช้

- **บันทึก** — บันทึกชื่อที่แสดงและสิทธิ์
- **ระงับการใช้งาน** — ล็อกอินได้แต่ทำอะไรไม่ได้เลย (เหมาะกับพนักงานที่ลาออก แต่ยังไม่อยากลบ)
- **ลบสิทธิ์** — ลบออกจากระบบสิทธิ์ (บัญชีล็อกอินยังอยู่ใน Supabase ต้องไปลบเองถ้าต้องการลบถาวร)

### ข้อควรรู้

- **เจ้าของระบบมีสิทธิ์ทุกอย่างเสมอ** ปรับสิทธิ์ไม่ได้ และลบตัวเองไม่ได้ (กันระบบล็อกตัวเอง)
- **สิทธิ์ที่เปลี่ยนจะมีผลเมื่อผู้ใช้คนนั้นออกจากระบบแล้วเข้าใหม่**
- สิทธิ์ถูกบังคับที่ฐานข้อมูล ไม่ใช่แค่ซ่อนปุ่ม — คนที่ไม่มีสิทธิ์จะถูกปฏิเสธแม้พยายามเรียกข้อมูลตรงๆ

---

## 8. ปัญหาที่พบบ่อย

### "Bucket not found"
ที่เก็บไฟล์ยังไม่ถูกสร้าง → Supabase → **Storage** → **New bucket** → ตั้งชื่อตามที่ระบบต้องการ → เปิด **Public bucket**

ระบบใช้ทั้งหมด 4 ที่เก็บ: `documents`, `review-logos`, `app-releases`, `article-images`

### "violates row-level security policy"
สองสาเหตุ:
1. **ยังไม่ได้รันไฟล์ SQL** → รัน `supabase-migration.sql` ใน Supabase SQL Editor
2. **บัญชีไม่มีสิทธิ์ในส่วนนั้น** → ให้เจ้าของระบบเปิดสิทธิ์ให้

### "Could not find the table ... in the schema cache"
ยังไม่ได้รันไฟล์ SQL หรือรันไม่จบ → รัน `supabase-migration.sql` ใหม่ทั้งไฟล์ (รันซ้ำได้ปลอดภัย)

### แก้อะไรแล้วหน้าเว็บไม่เปลี่ยน
กด **Ctrl + Shift + R** (Mac: **Cmd + Shift + R**) เพื่อล้างแคชเบราว์เซอร์

### บทความเขียนแล้วไม่ขึ้นเว็บ
1. ติ๊ก "เผยแพร่" หรือยัง
2. รอครบ 1 ชั่วโมงหรือยัง (หรือสั่ง Run workflow เองตามวิธีในหัวข้อบทความ)

### รีวิวอนุมัติแล้วแต่ไม่ขึ้นหน้าเว็บ
ต้องมีรีวิวที่อนุมัติแล้ว **อย่างน้อย 5 รายการ** ถึงจะแสดง

### ลืมรหัสผ่านเข้าหลังบ้าน
Supabase → **Authentication** → **Users** → คลิกที่ผู้ใช้ → **Reset password**

---

## ข้อมูลระบบ

| รายการ | ค่า |
|---|---|
| เว็บไซต์ | www.dcbc.co.th |
| หลังบ้าน | www.dcbc.co.th/adminupload |
| ระบบเอกสารลูกค้า | www.dcbc.co.th/portal |
| หน้าส่งรีวิว | www.dcbc.co.th/review |
| หน้าบทความ | www.dcbc.co.th/articles |
| หน้าดาวน์โหลดโปรแกรม | www.dcbc.co.th/program/downloader |
| ฐานข้อมูล | Supabase |
| โค้ดเว็บไซต์ | GitHub — `dcbc8878/web` |
